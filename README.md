### arch-hyprland

Run `archinstall`
- Bootloader: "GRUB"
- Profile: "Minimal"
- Network configuration: "Use NetworkManager"

```
sudo pacman -S git --noconfirm && \
git clone https://github.com/chriscorbell/arch-hyprland && \
cd arch-hyprland && \
./install.sh
```
